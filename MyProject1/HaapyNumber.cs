using System;
using System.Collections.Generic;
using System.Text;
/*
namespace MyProject1   //19=1*1+9*9=82   19 is Happy Number
{                      //82=8*8+2*2=68
                       // 68=6*6+8*8=100
                //100=1*1+0*0+0*0=1
class HaapyNumber
{
    static void Main(string[] args)
   {
       HappyNumber n1 = new HappyNumber();
       Console.WriteLine("Enter the Number");
       int n = int.Parse(Console.ReadLine());
       int temp = n;
       while (temp > 9)
       {
           temp = n1.sumSqDigit(temp);
       }
       if (temp == 1)
           Console.WriteLine(n + " is a Happy Number");
       else
           Console.WriteLine(n + "  is not a Happy Number");

       int sumSqDigit(int n)
       {
           int sum = 0;
           while (n != 0)
           {
               int d = n % 10;
               sum += d * d;
               n /= 10;
           }
           return sum;
       }
   }
}
}
*/

            
        
    

